<UserControl
    x:Class="Desktop.App.Controls.TrayGridControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:Desktop.App.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <converters:NullToCollapsedConverter x:Key="NullToCollapsedConverter" />

        <!-- Pulse animation for Picking state -->
        <Storyboard x:Key="PulseAnimation" RepeatBehavior="Forever">
            <DoubleAnimation
                Storyboard.TargetProperty="Opacity"
                From="1.0"
                To="0.4"
                Duration="0:0:0.6"
                AutoReverse="True" />
        </Storyboard>

        <!-- Slot item template -->
        <DataTemplate x:Key="SlotTemplate">
            <Border
                Width="20"
                Height="20"
                Margin="2"
                CornerRadius="10">
                <Border.ToolTip>
                    <StackPanel MaxWidth="200">
                        <TextBlock FontWeight="SemiBold" Text="{Binding Position, StringFormat='Vị trí {0}'}" />
                        <TextBlock
                            Foreground="#475569"
                            Text="{Binding OrderId, StringFormat='Order: {0}'}"
                            Visibility="{Binding OrderId, Converter={StaticResource NullToCollapsedConverter}}" />
                        <TextBlock
                            Foreground="#475569"
                            Text="{Binding ModelName, StringFormat='Model: {0}'}"
                            Visibility="{Binding ModelName, Converter={StaticResource NullToCollapsedConverter}}" />
                    </StackPanel>
                </Border.ToolTip>
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="#E2E8F0" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Status}" Value="HasProduct">
                                <Setter Property="Background" Value="#2563EB" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Status}" Value="Picking">
                                <Setter Property="Background" Value="#16A34A" />
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard RepeatBehavior="Forever">
                                            <DoubleAnimation
                                                Storyboard.TargetProperty="Opacity"
                                                From="1.0"
                                                To="0.4"
                                                Duration="0:0:0.6"
                                                AutoReverse="True" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Border
        Background="#F8FAFC"
        BorderBrush="#E2E8F0"
        BorderThickness="1"
        CornerRadius="8"
        Padding="10,8">
        <StackPanel>
            <!-- Tray label -->
            <TextBlock
                x:Name="TrayLabelText"
                Margin="0,0,0,4"
                FontSize="13"
                FontWeight="SemiBold"
                Foreground="#64748B"
                Text="{Binding TrayLabel, RelativeSource={RelativeSource AncestorType=UserControl}}" />

            <!-- Grid of slots -->
            <ItemsControl
                x:Name="SlotsItemsControl"
                ItemTemplate="{StaticResource SlotTemplate}"
                ItemsSource="{Binding SlotStates, RelativeSource={RelativeSource AncestorType=UserControl}}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid
                            x:Name="SlotsGrid"
                            Columns="{Binding GridColumns, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            Rows="{Binding GridRows, RelativeSource={RelativeSource AncestorType=UserControl}}">
                            <UniformGrid.LayoutTransform>
                                <ScaleTransform ScaleY="-1" />
                            </UniformGrid.LayoutTransform>
                        </UniformGrid>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </StackPanel>
    </Border>
</UserControl>
