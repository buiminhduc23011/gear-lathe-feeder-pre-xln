<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:primitives="clr-namespace:System.Windows.Controls.Primitives;assembly=PresentationFramework">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Tokens.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="IndustrialCommandButton" TargetType="Button">
        <Setter Property="MinHeight" Value="{DynamicResource Size.ServoButton.Height}" />
        <Setter Property="MinWidth" Value="120" />
        <Setter Property="Padding" Value="18,10" />
        <Setter Property="FontFamily" Value="{DynamicResource FontFamily.App}" />
        <Setter Property="FontSize" Value="{DynamicResource FontSize.ServoButton}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Background" Value="{DynamicResource Brush.Surface.Panel}" />
        <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border.Strong}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border
                        x:Name="Root"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{DynamicResource Radius.Button}">
                        <ContentPresenter
                            Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            TextElement.Foreground="{TemplateBinding Foreground}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Opacity" Value="0.94" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="Opacity" Value="0.86" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ButtonPrimary" TargetType="Button" BasedOn="{StaticResource IndustrialCommandButton}">
        <Setter Property="Background" Value="{DynamicResource Brush.Action.Primary}" />
        <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Inverse}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Action.Primary}" />
    </Style>

    <Style x:Key="ButtonSecondary" TargetType="Button" BasedOn="{StaticResource IndustrialCommandButton}">
        <Setter Property="Background" Value="{DynamicResource Brush.Surface.Subpanel}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border.Strong}" />
    </Style>

    <Style x:Key="ButtonDanger" TargetType="Button" BasedOn="{StaticResource IndustrialCommandButton}">
        <Setter Property="Background" Value="{DynamicResource Brush.Action.DangerSurface}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Action.Danger}" />
        <Setter Property="Foreground" Value="{DynamicResource Brush.Action.Danger}" />
    </Style>

    <Style x:Key="ServoButton" TargetType="Button" BasedOn="{StaticResource IndustrialCommandButton}" />

    <Style x:Key="ServoButton.Start" TargetType="Button" BasedOn="{StaticResource ButtonPrimary}">
        <Setter Property="Background" Value="{DynamicResource Brush.Action.Confirm}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Action.Confirm}" />
        <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Inverse}" />
    </Style>

    <Style x:Key="ServoButton.Stop" TargetType="Button" BasedOn="{StaticResource ButtonDanger}" />

    <Style x:Key="ServoButton.Jog" TargetType="Button" BasedOn="{StaticResource ButtonSecondary}">
        <Setter Property="MinWidth" Value="96" />
    </Style>

    <Style x:Key="ShellActionButton" TargetType="Button" BasedOn="{StaticResource ButtonSecondary}">
        <Setter Property="MinWidth" Value="116" />
        <Setter Property="MinHeight" Value="38" />
        <Setter Property="Padding" Value="14,8" />
        <Setter Property="FontSize" Value="{DynamicResource FontSize.Caption}" />
    </Style>

    <Style x:Key="HeaderCompactButton" TargetType="Button" BasedOn="{StaticResource ButtonSecondary}">
        <Setter Property="MinWidth" Value="92" />
        <Setter Property="MinHeight" Value="34" />
        <Setter Property="Padding" Value="12,6" />
        <Setter Property="FontSize" Value="{DynamicResource FontSize.Caption}" />
    </Style>

    <Style x:Key="HeaderCompactPrimaryButton" TargetType="Button" BasedOn="{StaticResource ButtonPrimary}">
        <Setter Property="MinWidth" Value="104" />
        <Setter Property="MinHeight" Value="34" />
        <Setter Property="Padding" Value="12,6" />
        <Setter Property="FontSize" Value="{DynamicResource FontSize.Caption}" />
    </Style>

    <Style x:Key="PagerButton" TargetType="Button" BasedOn="{StaticResource ButtonSecondary}">
        <Setter Property="MinWidth" Value="42" />
        <Setter Property="MinHeight" Value="42" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="FontSize" Value="{DynamicResource FontSize.Caption}" />
    </Style>

    <Style x:Key="PillToggleButton" TargetType="primitives:ToggleButton">
        <Setter Property="MinHeight" Value="38" />
        <Setter Property="MinWidth" Value="118" />
        <Setter Property="Padding" Value="14,8" />
        <Setter Property="FontFamily" Value="{DynamicResource FontFamily.App}" />
        <Setter Property="FontSize" Value="{DynamicResource FontSize.Caption}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Background" Value="{DynamicResource Brush.Status.InfoSurface}" />
        <Setter Property="Foreground" Value="{DynamicResource Brush.Status.Info}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Status.Info}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="primitives:ToggleButton">
                    <Border
                        x:Name="Root"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{DynamicResource Radius.Badge}"
                        Padding="{TemplateBinding Padding}">
                        <ContentPresenter 
                            HorizontalAlignment="Center" 
                            VerticalAlignment="Center"
                            TextElement.Foreground="{TemplateBinding Foreground}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource Brush.Status.FaultSurface}" />
                            <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource Brush.Status.Fault}" />
                            <Setter Property="Foreground" Value="{DynamicResource Brush.Status.Fault}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="HeaderCompactToggleButton" TargetType="primitives:ToggleButton" BasedOn="{StaticResource PillToggleButton}">
        <Setter Property="MinWidth" Value="108" />
        <Setter Property="MinHeight" Value="34" />
        <Setter Property="Padding" Value="12,6" />
        <Setter Property="FontSize" Value="{DynamicResource FontSize.Caption}" />
    </Style>

    <Style x:Key="WindowChromeButton" TargetType="Button">
        <Setter Property="Width" Value="{DynamicResource Size.WindowButton}" />
        <Setter Property="Height" Value="{DynamicResource Size.WindowButton}" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="FontFamily" Value="{DynamicResource FontFamily.App}" />
        <Setter Property="FontSize" Value="{DynamicResource FontSize.Body}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Background" Value="{DynamicResource Brush.Surface.Subpanel}" />
        <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border.Default}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border
                        x:Name="Root"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{DynamicResource Radius.Button}">
                        <ContentPresenter 
                            HorizontalAlignment="Center" 
                            VerticalAlignment="Center"
                            TextElement.Foreground="{TemplateBinding Foreground}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource Brush.Surface.Hover}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="Opacity" Value="0.86" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="WindowChromeToggleButton" TargetType="primitives:ToggleButton">
        <Setter Property="Width" Value="{DynamicResource Size.WindowButton}" />
        <Setter Property="Height" Value="{DynamicResource Size.WindowButton}" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="FontFamily" Value="{DynamicResource FontFamily.App}" />
        <Setter Property="FontSize" Value="{DynamicResource FontSize.Caption}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Background" Value="{DynamicResource Brush.Surface.Subpanel}" />
        <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border.Default}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="primitives:ToggleButton">
                    <Border
                        x:Name="Root"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{DynamicResource Radius.Button}">
                        <ContentPresenter 
                            HorizontalAlignment="Center" 
                            VerticalAlignment="Center"
                            TextElement.Foreground="{TemplateBinding Foreground}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource Brush.Action.PrimarySurface}" />
                            <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource Brush.Action.Primary}" />
                            <Setter Property="Foreground" Value="{DynamicResource Brush.Action.Primary}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
